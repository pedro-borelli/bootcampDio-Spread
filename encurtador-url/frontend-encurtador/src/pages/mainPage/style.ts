import styled from "styled-components";

export const Main = styled.main`
    background: linear-gradient(180deg, #000000 0%, #15162D 100%);
    height:100vh;
    position: relative;
    overflow: hidden;
`